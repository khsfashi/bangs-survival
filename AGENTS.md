# Bangs Survival Agent Guide

이 문서는 새 세션에서 프로젝트 상태를 빠르게 복구하고 다음 작업을 결정하기 위한 기준 문서입니다.

## 제품 목표

한국 사용자가 외출 전에 `오늘 앞머리를 세팅할 가치가 있는가?`를 빠르게 판단하도록 돕습니다. 범용 헤어/날씨 앱이 아니라 앞머리 유지 의사결정에 집중합니다.

## 다음 작업 요청을 받았을 때

사용자가 `@GitHub bangs-survival 다음 작업 진행해줘`, `이어서 진행해줘`처럼 요청하면 다음 순서로 진행합니다.

1. open PR을 확인하고 미완료 작업이나 CI 실패를 먼저 확인합니다.
2. open issue를 확인하고 우선순위와 의존성을 판단합니다.
3. `PRIOR_ART.md`와 관련 문서를 읽어 이미 존재하는 상용 앱, 연구, 오픈소스를 확인합니다.
4. 기능을 바로 구현하기 전에 필요한 최신 공식 문서나 데이터 공급자 상태를 확인합니다.
5. 기능 브랜치에서 구현합니다. `main`에 직접 기능 개발을 하지 않습니다.
6. 결정론적 로직은 DOM/API와 분리하고 Node 내장 테스트 러너로 회귀 테스트를 추가합니다.
7. 테스트와 가능한 브라우저/배포 검증을 수행한 뒤 PR을 엽니다.
8. 사용자가 명시적으로 요청하지 않았다면 PR을 임의로 merge하지 않습니다.

## Prior-art gate

새 기능은 `Need -> Research -> OSS/Commercial -> Gap -> Feasibility -> Build/Don't Build` 순서로 검토합니다. 바퀴를 재발명하지 않습니다. 참조한 논문, 공식 문서, 오픈소스, 상용 서비스는 `PRIOR_ART.md` 또는 PR 본문에 남깁니다.

## 기술 원칙

- deterministic 계산을 우선하고, LLM은 필요성이 검증될 때만 도입합니다.
- 계정/서버 DB가 필요하지 않다면 local-first를 유지합니다.
- API 비밀키와 서버 전용 키는 저장소에 커밋하지 않습니다.
- 브라우저에 노출되는 키도 provider의 도메인 제한을 적용합니다.
- 브라우저 geolocation의 reported accuracy를 절대적인 실제 오차 보증으로 취급하지 않습니다. 특히 PC에서는 사용자 확인 경로를 제공합니다.
- 위치 좌표는 서버 DB에 저장하지 않습니다. 다만 사용자가 지도에서 명시적으로 확정한 최근 위치는 다음 사용 편의를 위해 현재 브라우저 localStorage에만 저장할 수 있으며 UI/README에 이를 명시합니다.
- 수동 확인 위치와 새 geolocation이 크게 충돌하면 자동 선택하지 않고 사용자에게 선택권을 줍니다.
- 외부 API 실패 시 사용자에게 정확한 fallback 상태를 보여줍니다.

## 테스트

```bash
npm test
```

현재 핵심 회귀 항목은 위험도 계산, 판정/캐릭터 단계, KMA 변환/파싱, 위치 점프 판정, 저장 위치 정규화, 하루 1회 피드백 upsert, 개인 보정 시작 조건입니다.
